pub struct Renderer {
    // Rendering details
}

impl Renderer {
    fn new() -> Self {
        // initialize rendering resources
    }

    fn  render(&self, world: &World) {
        // Render world and its entities
    }
}